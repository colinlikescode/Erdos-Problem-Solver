# Prebuilt sandbox runtime image (-python variant: python3 preinstalled).
# The version MUST match the installed @cloudflare/sandbox npm package version
# (check node_modules/@cloudflare/sandbox/package.json).
FROM docker.io/cloudflare/sandbox:0.12.3-python

EXPOSE 8080
