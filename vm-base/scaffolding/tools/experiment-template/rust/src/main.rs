//! CPU search / construction for the problem in `problem.md` (snapshot root).
//!
//! This is a starting point, not a solution. The real work - your search and
//! construction code for this experiment - goes here in Rust (and in `../cuda`
//! when a GPU is available). Constructions must pass the independent answer
//! checker in `check_answer/` before anything is recorded in `verified_math/`.
//!
//! Keep this crate for *searching and constructing* candidates; keep the
//! *checking* logic in `check_answer/` so the two never share bugs.

fn main() {
    println!("experiment: implement construction/search here.");
    println!("See AGENTS.md and problem.md at the snapshot root.");
    println!("Build the answer checker in check_answer/ FIRST, then verify every candidate.");
}
