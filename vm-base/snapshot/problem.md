# Problem

_(Fill this in with the actual problem and nothing else: the precise target and
what counts as a correct, verified solution. No background, no breakdown into
steps, no hints, no methods. How to work the problem lives in
[`AGENTS.md`](./AGENTS.md).)_

## The problem

The problem is X.

## Definition of "solved"

Y: the exact condition a correct answer must satisfy. This is what the answer
checker in `check_answer/` enforces and what the winning `verified_math/` entry
records.
